#include <stdio.h>
int main(void)
{
    int arr[10];
    int n =0;
    
    for (int i = 0; i < 10; i++)
    {
        scanf("%d",&arr[i]);
        if (arr[i] >= n)
        {
            n = arr[i];
        }
    }
    printf("%d\n",n);
    return 0;
}
